import { defineConfig } from 'vite';

export default defineConfig({
  root: './', // Specify the root directory (if not the default).
  build: {
    outDir: 'dist', // The output directory for the build.
  },
});